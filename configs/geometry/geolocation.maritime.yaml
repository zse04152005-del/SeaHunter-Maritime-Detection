schema_version: 1
sea_plane:
  minimum_downward_component: 0.03
  maximum_range_m: 5000.0
  minimum_absolute_quality: 0.25
  maximum_calibration_rms_px: 1.0
  range_sigma_scale_m: 50.0
uncertainty_defaults:
  platform_altitude_sigma_m: 2.0
  tide_sigma_m: 0.5
  wave_sigma_m: 1.0
  attitude_sigma_deg: 1.0
filter:
  acceleration_sigma_m_s2: 2.0
  initial_velocity_sigma_m_s: 10.0
  maximum_gap_seconds: 5.0
trend:
  stationary_speed_m_s: 0.5
  radial_rate_threshold_m_s: 0.5
  minimum_ttc_quality: 0.6
  maximum_ttc_seconds: 300.0

# Starting gates only. Freeze by aircraft/camera profile and validate against
# RTK/AIS/rangefinder/known-marker truth before any operational distance claim.
