tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.05
new_track_thresh: 0.30
track_buffer: 90
match_thresh: 0.80
fuse_score: true

# These are starting values only. They must be calibrated on video-level MOT data.
